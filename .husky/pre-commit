pnpm astro check

# Don't exit when command returns non-zero exit code
set +e

pnpm exec prettier --check . &> /dev/null

if [ $? -eq 1 ]; then
  pnpm exec prettier --write .
  echo "Auto-formatting changes detected. Please stage them and commit again."
  exit 1
fi
